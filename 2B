<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>AVS TEXTILES — Buy & Sell Fabrics & Textiles</title>
<style>
  /* Root colors */
  :root {
    --primary: #6b4f4f;
    --primary-light: #c7a5a5;
    --secondary: #f2e8e8;
    --accent: #d9615b;
    --text-dark: #3a2f2f;
    --text-muted: #7f6a6a;
    --border-radius: 14px;
    --shadow: 0 6px 18px rgba(107, 79, 79, 0.15);
    --transition: 0.3s ease;
  }

  /* Reset & base */
  *, *::before, *::after {
    box-sizing: border-box;
  }

  body {
    margin: 0; 
    font-family: 'Poppins', sans-serif;
    background: var(--secondary);
    color: var(--text-dark);
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }

  /* Header */
  header {
    background: var(--primary);
    color: var(--secondary);
    padding: 20px 30px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    box-shadow: var(--shadow);
    border-bottom-left-radius: 24px;
    border-bottom-right-radius: 24px;
  }
  header .logo {
    font-size: 28px;
    font-weight: 800;
    letter-spacing: 2px;
    font-family: 'Courier New', Courier, monospace;
  }
  header .tagline {
    font-weight: 300;
    font-size: 14px;
    opacity: 0.8;
    margin-top: 4px;
  }
  header input[type="search"] {
    border-radius: 18px;
    padding: 8px 16px;
    border: none;
    width: 280px;
    font-size: 15px;
    transition: box-shadow var(--transition);
  }
  header input[type="search"]:focus {
    outline: none;
    box-shadow: 0 0 8px var(--accent);
  }
  header button {
    margin-left: 15px;
    padding: 10px 20px;
    background: var(--accent);
    border: none;
    border-radius: 18px;
    color: var(--secondary);
    font-weight: 600;
    cursor: pointer;
    transition: background-color var(--transition);
  }
  header button:hover {
    background: #b84c47;
  }

  /* Main layout */
  .container {
    max-width: 1100px;
    margin: 25px auto;
    display: flex;
    gap: 30px;
    padding: 0 15px;
  }

  /* Sidebar - form & filters */
  aside.sidebar {
    flex: 0 0 360px;
    background: white;
    border-radius: var(--border-radius);
    padding: 22px 24px;
    box-shadow: var(--shadow);
    height: fit-content;
    position: sticky;
    top: 30px;
  }
  aside.sidebar h2 {
    margin-top: 0;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 12px;
  }
  label {
    display: block;
    margin: 14px 0 6px;
    font-weight: 600;
    font-size: 13px;
    color: var(--text-muted);
  }
  input[type="text"],
  input[type="number"],
  select,
  textarea {
    width: 100%;
    padding: 10px 12px;
    border-radius: 12px;
    border: 1.5px solid #ddd;
    font-size: 14px;
    resize: vertical;
    transition: border-color var(--transition);
  }
  input[type="text"]:focus,
  input[type="number"]:focus,
  select:focus,
  textarea:focus {
    outline: none;
    border-color: var(--accent);
  }
  textarea {
    min-height: 80px;
  }
  button.primary-btn {
    margin-top: 18px;
    width: 100%;
    background: var(--primary);
    color: var(--secondary);
    font-weight: 700;
    border: none;
    padding: 12px 0;
    border-radius: 20px;
    cursor: pointer;
    transition: background-color var(--transition);
  }
  button.primary-btn:hover {
    background: #563b3b;
  }
  button.clear-btn {
    margin-top: 10px;
    width: 100%;
    background: transparent;
    border: 1.5px solid var(--primary);
    color: var(--primary);
    font-weight: 600;
    border-radius: 20px;
    cursor: pointer;
    transition: background-color var(--transition), color var(--transition);
  }
  button.clear-btn:hover {
    background: var(--primary);
    color: var(--secondary);
  }

  /* Filters */
  .filters {
    margin-top: 28px;
    border-top: 1.5px solid #eee;
    padding-top: 18px;
  }
  .filters h3 {
    margin: 0 0 12px;
    font-weight: 700;
    color: var(--primary);
  }
  .filters select {
    margin-bottom: 12px;
  }

  /* Listings area */
  main.listings-area {
    flex: 1;
  }
  .listings-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 18px;
  }
  .listings-header h2 {
    margin: 0;
    font-weight: 700;
    font-size: 22px;
    color: var(--primary);
  }
  .view-toggle button {
    background: transparent;
    border: none;
    font-weight: 700;
    font-size: 15px;
    padding: 6px 16px;
    border-radius: 18px;
    cursor: pointer;
    color: var(--text-muted);
    transition: background-color var(--transition), color var(--transition);
  }
  .view-toggle button.active {
    background: var(--primary);
    color: var(--secondary);
  }
  .view-toggle button:not(.active):hover {
    background: var(--primary-light);
    color: var(--primary);
  }

  /* Grid layout */
  .grid-listings {
    display: grid;
    grid-template-columns: repeat(auto-fill,minmax(240px,1fr));
    gap: 20px;
  }
  .card {
    background: white;
    border-radius: var(--border-radius);
    box-shadow: var(--shadow);
    overflow: hidden;
    display: flex;
    flex-direction: column;
    transition: transform 0.2s ease;
  }
  .card:hover {
    transform: translateY(-6px);
  }
  .card img {
    width: 100%;
    height: 160px;
    object-fit: cover;
    border-bottom: 2px solid var(--accent);
  }
  .card-content {
    padding: 14px 18px;
    flex-grow: 1;
    display: flex;
    flex-direction: column;
  }
  .card-content h3 {
    margin: 0 0 6px;
    font-weight: 700;
    font-size: 18px;
    color: var(--primary);
  }
  .card-content p.description {
    flex-grow: 1;
    font-size: 14px;
    color: var(--text-muted);
    margin: 0 0 10px;
  }
  .card-content .price {
    font-weight: 800;
    font-size: 17px;
    color: var(--accent);
    margin-bottom: 10px;
  }
  .card-content .category {
    font-size: 13px;
    font-weight: 600;
    color: var(--primary-light);
    margin-bottom: 10px;
  }
  .card-content .contact-btn {
    padding: 10px 0;
    border-radius: 20px;
    border: none;
    background: var(--primary);
    color: var(--secondary);
    font-weight: 600;
    cursor: pointer;
    transition: background-color var(--transition);
  }
  .card-content .contact-btn:hover {
    background: #563b3b;
  }

  /* Table layout */
  table {
    width: 100%;
    border-collapse: collapse;
    background: white;
    border-radius: var(--border-radius);
    overflow: hidden;
    box-shadow: var(--shadow);
  }
  thead {
    background: var(--primary-light);
  }
  th, td {
    padding: 12px 18px;
    font-weight: 500;
    color: var(--text-dark);
    border-bottom: 1px solid #eee;
    text-align: left;
  }
  tbody tr:hover {
    background: var(--secondary);
  }
  td img {
    width: 80px;
    height: 55px;
    object-fit: cover;
    border-radius: 10px;
  }
  .actions button {
    padding: 6px 14px;
    margin-right: 6px;
    border-radius: 18px;
    border: none;
    cursor: pointer;
    font-weight: 600;
    transition: background-color var(--transition);
  }
  .actions button.contact {
    background: var(--accent);
    color: white;
  }
  .actions button.edit {
    background: var(--primary-light);
    color: var(--primary);
  }
  .actions button.delete {
    background: #f56262;
    color: white;
  }
  .actions button.contact:hover {
    background: #b14b47;
  }
  .actions button.edit:hover {
    background: #967a7a;
    color: var(--primary);
  }
  .actions button.delete:hover {
    background: #d9534f;
  }
</style>
</head>
<body>

<header>
  <div class="logo">AVS TEXTILES</div>
  <div class="tagline">Buy & Sell Fabrics & Textiles</div>
  <input type="search" placeholder="Search fabrics..." />
  <button>Search</button>
</header>

<div class="container">
  <aside class="sidebar">
    <h2>Filter Fabrics</h2>
    <label for="fabric-type">Fabric Type</label>
    <select id="fabric-type">
      <option value="">All Types</option>
      <option value="cotton">Cotton</option>
      <option value="silk">Silk</option>
      <option value="linen">Linen</option>
    </select>

    <label for="price-min">Min Price</label>
    <input type="number" id="price-min" placeholder="0" />

    <label for="price-max">Max Price</label>
    <input type="number" id="price-max" placeholder="1000" />

    <button class="primary-btn">Apply Filters</button>
    <button class="clear-btn">Clear Filters</button>
  </aside>

  <main class="listings-area">
    <div class="listings-header">
      <h2>Fabric Listings</h2>
      <div class="view-toggle">
        <button class="active">Grid View</button>
        <button>List View</button>
      </div>
    </div>

    <div class="grid-listings">
      <!-- Example card -->
      <div class="card">
        <img src="https://via.placeholder.com/240x160" alt="Fabric Image" />
        <div class="card-content">
          <h3>Cotton Fabric</h3>
          <p class="description">Soft and breathable cotton fabric, perfect for summer wear.</p>
          <div class="price">$15 / yard</div>
          <div class="category">Cotton</div>
          <button class="contact-btn">Contact Seller</button>
        </div>
      </div>
      <!-- Add more cards as needed -->
    </div>
  </main>
</div>

</body>
</html>

